### Terraform Workshop

* Install the latest version of terraform
* Generate private-public key pair using ssh-keygen
* In variables.tf, give your access_key and secret_key of AWS
* In variables.tf, give the path of you public key file
* In variables.tf, give a name for your key-value pair as you want to be listed in AWS
